# Windows Validation History

This is the stable index for completed Windows Owner rounds. Preserve original dates, revisions, commands, evidence, and result states; never rewrite a historical `FAIL` as `PASS`. Detailed legacy records remain under `docs/validation/history/`.

| Date | Source revision | Scope | Result | Record |
|---|---|---|---|---|
| Existing history | See `docs/validation/windows.md` and `docs/validation/history/` | Legacy Windows validation rounds | Preserve original states | Archive before moving records here |
